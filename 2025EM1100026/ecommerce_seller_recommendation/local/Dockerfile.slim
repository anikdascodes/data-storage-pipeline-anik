# Slim Dockerfile for E-commerce Recommendation System
# Student: MSc Data Science & AI, Roll No: 2025EM1100026
# 
# This is a lightweight version that assumes Spark is already installed
# Use this if the reviewer's system already has Spark/Java

FROM ubuntu:22.04

# Set environment variables
ENV DEBIAN_FRONTEND=noninteractive
ENV PYSPARK_PYTHON=/usr/bin/python3

# Install only essential dependencies
RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /app

# Copy project files
COPY requirements.txt /app/
COPY pyproject.toml /app/

# Install Python dependencies only
RUN pip3 install --no-cache-dir -r requirements.txt

# Copy the entire project
COPY . /app/

# Create necessary directories
RUN mkdir -p /app/processed /app/quarantine /app/logs

# Default command
CMD ["/bin/bash"]
